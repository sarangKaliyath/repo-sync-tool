{
  "name": "repo-sync-tool",
  "version": "1.3.0",
  "description": "A CLI tool to sync a source directory and merge package.json dependencies from one Git repository to another",
  "main": "sync.js",
  "bin": {
    "repo-sync": "sync.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "git",
    "sync",
    "repository",
    "cli",
    "devtools",
    "repo-sync",
    "file-sync",
    "package-merge"
  ],
  "author": "sarang-kaliyath",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "https://github.com/sarang-kaliyath/repo-sync-tool.git"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "fs-extra": "^11.3.5",
    "ora": "^9.4.0",
    "simple-git": "^3.36.0"
  }
}
